<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Person Found</title>

  <link rel="stylesheet" href="/public/site.css" />
</head>

<body>
  <main>
    <h1>People Found</h1>
    <h2>{{name}}</h2>
    {{#if people.length}}
    <ul>
      {{#each people}}
      <li>
        <a href="/details/{{id}}">{{firstName}} {{lastName}}</a>
      </li>
      {{/each}}
    </ul>
    {{else}}
      <p class="not-found">We're sorry, but no results were found for {{name}}.</p>
    {{/if}}
    <a href="/">Make another search</a>
  </main>
</body>

</html>